# supabase-appwrite

A project exploring Supabase and Appwrite integration.

## Project Overview

- **Purpose**: Experiment with Supabase and Appwrite backends, compare features, or build a dual-provider setup.
- **Tech Stack**: JavaScript/Node.js (add your framework here).
- **Repository**: `d:\Programming\supabase-appwrite`

## Claude Code Guidelines

- Prefer `npm` for package management.
- Run `git status` before and after file edits.
- Keep Mermaid diagrams in `.mmd` files under `docs/diagrams/`.
- Use the custom commands in `.claude/commands.json` for repetitive tasks.

## Common Commands

```bash
# Check status
git status

# Install dependencies
npm install

# Run the project (customize as needed)
npm start
```
